version: '3.8'

services:
  weather-api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - WEATHER_API_KEY=${WEATHER_API_KEY:-demo_key}
    restart: unless-stopped
    networks:
      - weather-network

networks:
  weather-network:
    driver: bridge